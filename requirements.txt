# ── Environment ─────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Core LangChain ───────────────────────────────────────────
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.60,<1.0.0
langchain-ollama>=0.3.0
langchain-text-splitters>=0.3.0
langsmith>=0.2.0
pydantic>=2.7.4
numpy>=1.26.2

# ── FastAPI backend ───────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # file upload support

# ── Streamlit frontend ────────────────────────────────────────
streamlit>=1.35.0
httpx>=0.27.0                # Streamlit → FastAPI HTTP client

# ── LLM Providers (install only what you need) ────────────────
# langchain-openai>=0.1.0       # for openai + azure_openai
# langchain-anthropic>=0.1.0    # for anthropic / claude
langchain-google-genai>=1.0.0  # for gemini

# ── MCP support (optional) ────────────────────────────────────
# langchain-mcp-adapters>=0.1.0
# mcp>=1.0.0
langraph>=1.1.10